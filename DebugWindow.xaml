<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="LibmpvIptvClient.DebugWindow"
        Title="{DynamicResource UI_DebugLog}" Width="900" Height="500" WindowStartupLocation="CenterOwner" Icon="pack://application:,,,/srcbox.ico">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="TxtLog" Grid.Row="0" FontFamily="Consolas" FontSize="12" IsReadOnly="True" TextWrapping="NoWrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Background="{DynamicResource AppBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderThickness="0"/>
        <DockPanel Grid.Row="1" LastChildFill="False" Margin="8">
            <Button Content="{DynamicResource UI_Clear}" Width="80" Click="BtnClear_Click" Style="{StaticResource ChipButton}"/>
        </DockPanel>
    </Grid>
    </Window>
